name: Validate Template

on:
  push:
    branches:
      - main

jobs:
  validate_xml:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@main

      - name: Validate XML files
        uses: action-pack/valid-xml@master
        with:
          path: "templates/"
          file-endings: ".xml"
